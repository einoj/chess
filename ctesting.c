#include <stdio.h>
/*
void swap (int *v1, int *v2)
{
	int temp;
	temp = *v1;
	*v1 = *v2;
	*v2 = temp;
	if (*v2 == 2) {

	printf("%d %d", *v1, *v2);
	}
}

int main (void)
{
	int a = 3, b = 4;

	printf("Før:	a = %d og b = %d\n", a, b);
	swap(&a, &b);
	printf("Etter: a = %d og b = %d\n", a, b);
}
*/
int main(void) {
	while(1) {
		unsigned char input[10];
		printf("white's move: ");
		fgets(input, 5, stdin);
	}
}
